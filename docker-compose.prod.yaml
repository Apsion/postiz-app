# Production Docker Compose for Postiz
# Deployment target: Dokploy (dokploy.apsion.com)
# Application URL: https://postiz.apsion.com

services:
  postiz:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        NEXT_PUBLIC_VERSION: '1.0.0'
    restart: unless-stopped
    expose:
      - "5000"
    environment:
      # Environment variables are configured in Dokploy's env editor
      # See .env.production.template for required variables
      - NODE_ENV=production
      - DISABLE_REGISTRATION=true
    volumes:
      - uploads:/uploads  # Local file storage (optional if using Cloudflare R2)
    depends_on:
      postiz-postgres:
        condition: service_healthy
      postiz-redis:
        condition: service_healthy
    networks:
      postiz-network:
        priority: 1000        # Bridge — always available on reboot
      dokploy-network:
        priority: 100         # Overlay — available after Dokploy starts
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://localhost:5000/']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s  # Allow time for build and PM2 startup

  postiz-postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postiz
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: postiz
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - postiz-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postiz']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    # No exposed ports - internal only

  postiz-redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data  # Persist queue data
    networks:
      - postiz-network
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    # No exposed ports - internal only

volumes:
  postgres-data:
    driver: local
  redis-data:
    driver: local
  uploads:
    driver: local

networks:
  postiz-network:
    driver: bridge
  dokploy-network:
    external: true
